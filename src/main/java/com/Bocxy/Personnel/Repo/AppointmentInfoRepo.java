package com.Bocxy.Personnel.Repo;

import com.Bocxy.Personnel.Entity.AppointmentInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AppointmentInfoRepo extends JpaRepository<AppointmentInfo, Long> {
}
